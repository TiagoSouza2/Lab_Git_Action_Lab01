name: Hello Flow
on:
 schedule:
  - cron: '*/5 * * * *'
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: Print greeting
      run: echo "Hello world"
    - name: Good bye
      run: echo "This job step is over, Bye bye.!"
